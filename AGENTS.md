# AGENTS.md — 选题B：高性能芯片热管理系统的优化问题

本文件是本项目（数学建模大赛·选题B）的持久化工作约定与已定建模思路。后续所有工作请遵守本文件，除非用户明确要求修改。

## 1. 项目背景

- 题目：高性能芯片热管理系统的优化问题（歧管式微通道 + 针肋强化）。
- 目标：用数学建模方法完成 5 个问题（Q1 机理建模、Q2 代理模型、Q3 多目标优化、Q4 权重敏感性与鲁棒设计、Q5 加工误差/工况波动下的稳定性评估）。
- 材料位置（只读，勿修改）：
  - `E:\数学建模大赛\选题D\选题B\选题B.pdf`（赛题）
  - `E:\数学建模大赛\选题D\选题B\附件\附件 1：芯片歧管式微通道热管理系统结构参数说明(3).pdf`
  - `E:\数学建模大赛\选题D\选题B\附件\附件 2：不同结构参数下无量纲的热阻、压降和温度非均匀性结果数据.xlsx`
- 工作目录：`E:\选题B`（所有生成物写到这里）。
- 参考思路来源：用户提供的《1.1.doc》（第一问机理建模参考，公式为位图，已用 OCR 还原），**其思路已确认采用**。

## 2. 关键问题定义

- 结构参数（自变量）：
  - w：针肋宽度比（针肋直径/通道宽度），范围 0~0.3；
  - r：歧管深高比（歧管层/微通道层深度比），范围 3~4.5；
  - n：单个歧管单元内沿流向的针肋排数，0~10 整数。
- 性能指标（因变量，无量纲，**越小越好**）：R 热阻、P 压降、U 温度非均匀性。
- 物理约束：w=0（无针肋）时 n 只能为 0，即「w=0 ⇒ n=0」；代理模型不得外推到 w=0 且 n>0 的假想工况。

## 3. 附件 2 数据要点（84 组样本，EDA 已确认）

- 设计空间为网格：w ∈ {0, 0.1, 0.15, 0.2, 0.3}，r ∈ {3, 3.5, 4, 4.5}，n ∈ {0,2,4,6,8,10}；其中 w=0 仅与 n=0 搭配（4 组），其余 80 组为 w>0 全组合。
- 指标范围：R ∈ [0.7219, 0.7738]（相对变化约 7%）、P ∈ [0.0767, 0.2040]（约 2.7 倍）、U ∈ [0.7740, 0.8731]（约 13%）。P 对参数最敏感，R 最不敏感。
- 相关关系（Pearson）：
  - w：R −0.32，P +0.53，U −0.15；
  - r：R +0.54，P −0.62，U −0.22；
  - n：R −0.50，P +0.43，U +0.30；
  - R–P −0.68（强冲突）、P–U +0.47、R–U −0.08（近独立）。
- 单目标最优样本：(w,r,n) 最小 R=(0.2,3.0,10)；最小 P=(0.2,4.5,2)；最小 U=(0.2,4.5,4)。
- 84 组数据中有 12 个 Pareto 前沿点，几乎全部集中在 w=0.2 附近——综合/鲁棒最优大概率落在 w≈0.2、r≈3.5~4.5、n≈4~6 区域。

## 4. 已确定的第一问建模思路（采用参考文档 1.1.doc）

### A. 物理模型构建

**热阻模型（串联热阻分解）**：R_total = R_cond + R_conv + R_fluid
- 导热热阻：R_cond = δ_sub / (k_AlN · A)（氮化铝衬底）；
- 对流换热热阻：R_conv = 1 / (h · A_eff)；
- 流体升温热阻（参考思路的亮点，勿遗漏）：R_fluid = 1 / (2·ṁ·Cp)；
- 针肋强化对流换热系数 h：用 Nu 数关联式，明确采用 **Zukauskas 针肋管束公式修正**：Nu = C·Re^m·Pr^0.36·(Pr/Pr_w)^0.25 · f(d_pin/w_ch, …)，修正因子 f 与 w、n 相关。

**压降模型（多段串联）**：ΔP = ΔP_manifold + ΔP_channel + ΔP_pin + ΔP_turn
- 沿程阻力：ΔP_f = f·(L/D_h)·(ρV²/2)（Darcy–Weisbach）；
- 针肋局部阻力：ΔP_pin ∝ C_D·(ρV²/2)，与 w、n 相关；
- 歧管入口/转弯损失：ΔP_turn = K_turn·(ρV²/2)，与 r 相关。

**温度非均匀性模型**
- 指标定义：温度标准差 σ_T 或最大温差 (T_max − T_min)；
- 两大主导因素：流量分配均匀性（主要由 r 决定）+ 局部强化均匀性（主要由 w、n 决定）。

### B. 第一问仍需补充完成的部分

1. 用机理公式推导各参数对 R、P、U 的影响规律（单调性、强弱、拐点），并与附件 2 数据趋势对照验证（趋势一致是关键加分点）。
2. 论证三项指标作为综合评价依据的合理性：工程维度（散热能力/能耗/可靠性）互补 + 统计维度（R–P 冲突、R–U 近独立）不可替代 + 无量纲形式便于跨尺度比较。
3. 明确无量纲 R、P、U 的定义并全程沿用（题目未给显式定义，论文需自洽说明）。

## 5. 后续问题路线（保持主线一致）

- Q2 代理模型：对 (w,r,n)→(R,P,U) 建模，候选 RSM 多项式/克里金/RBF/SVR/随机森林/XGBoost/BP；小样本用留一交叉验证（LOOCV）或 K 折，报告 R²/RMSE/MAE，防止过拟合；可引入 Q1 的幂律/物理先验。
- Q3 多目标优化：先 min-max 归一化，加权和（等权/熵权/AHP）或 NSGA-II 求 Pareto 前沿再选折中解；n 按整数处理；最优解需回代代理模型验证。
- Q4 权重敏感性：在 α+β+γ=1 单纯形上扫描权重，做"最优设计随权重变化"的分区图；鲁棒判据用最小最大后悔（minimax regret）/占优区域占比/最坏情况最优。
- Q5 不确定性：区分加工误差（w、r、n 容差）与工况波动（流量/入口温度/热流密度）；局部灵敏度 + 蒙特卡洛（+Sobol' 方差分解）；用 Q4 鲁棒方案与其他方案对比性能退化。

## 6. 附件 1 物性/工况（供机理模型定量化）

- 去离子水：ρ=998.2 kg/m³，Cp=4182 J/(kg·K)，k=0.6 W/(m·K)，μ=0.001003 kg/(m·s)；
- 氮化铝（衬底）：ρ=3260，Cp=700，k=200；芯片等效材料：ρ=19320，Cp=130，k=298；
- 工况：入口质量流量 1 g/s、入口温度 293 K、出口表压 0 Pa、环境 293 K、外侧自然对流 h=10 W/(m²·K)、芯片体热源强度 5×10^9 W/m³；
- 芯片散热区 10 mm×10 mm，视为均匀体热源。

## 7. 工作约定

- 交互与文档语言：中文；代码注释可用中英混合。
- 原始材料只读；所有输出（报告、代码、图表）写入 `E:\选题B`。
- 数据文件读取用 openpyxl/pandas；PDF 用 pdfplumber；图表若需 matplotlib 需先安装。
- 修改本文件或改变已定思路前，先与用户确认。
